# 任务：重新设计并实现 Papertrail 的前端

你在一个独立的 git worktree 里工作，分支 `frontend-redesign`。只改 `apps/web/`，
不要动 `apps/api/`、Dockerfile、workflow、schemas。

## 产品是什么

Papertrail 是本地、确定性、非破坏式的**文档改字工具**。上传 PDF，改里面的文字，导出新版本，
源文件永不修改。核心特点是**按页面类型分两条路径**：

| 页面类型 | layout.kind | 怎么改 | 用户看到的差别 |
| --- | --- | --- | --- |
| 矢量页（数字生成的 PDF） | `"vector"` | 页面里有真正的文字对象，点选后替换/移动/删除，导出无损 | 文字可选中，改完仍是文字 |
| 位图页（扫描件/截图） | `"raster"` | 整页只有像素。先 OCR 出文字框，再擦掉原字、用匹配到的字体重绘 | 需要先点「识别文字」 |

这条分界线是硬的：位图操作落到矢量页上会被服务端拒绝（`RASTER_EDIT_ON_VECTOR_PAGE`），
反之矢量操作也不能指向位图框。**设计上要让用户一眼看出当前页属于哪一类、能做什么。**

## 不可改变的契约（改了就跑不通）

所有编辑都是**操作日志**（operation log），不是直接改文档。前端攒一串 operation，
保存到草稿（带 revision 乐观锁），导出时服务端归并后一次性应用。

- `apps/web/src/lib/` 下的模块是已测试的逻辑层，**保留它们的导出签名和行为**：
  `api.ts`（HTTP 客户端）、`operationState.ts`（矢量元素推导）、`rasterState.ts`（位图元素推导与操作组装）、
  `history.ts`（撤销重做）、`exportWorkflow.ts`（保存+导出，含 409 重试）、`fitBbox.ts`
- `apps/web/src/types.ts` 是与后端共享的类型，可以加字段但不要改现有语义
- 现有测试 `npm test` 必须继续全绿。你可以新增测试，不要删除或放宽现有断言
- 坐标一律是 **PDF 点**（不是像素）。画布按 scale 换算显示
- 位图框不能拖动（没有对应的 PDF 文字对象，move_text 指向它会被 reducer 拒绝）

## 你要重做的部分

`App.tsx`（412 行，状态和 UI 混在一起）和 `components/PdfViewer.tsx`，以及 `styles.css`。
可以自由拆分组件、重组状态、换布局和视觉。目标：

1. **信息层次更清楚**。现在检查器里矢量和位图两套表单堆在一起，页面类型只在左侧列表里用一行小字表示。
2. **位图流程要顺**：识别 → 选框 → 看匹配结果 → 改字 → 应用。其中「原文可改后重新匹配」是关键动作
   （OCR 常吞掉标点后的空格，原文错了字号和字体就标定错），现在藏得很深。
3. **把诊断信息呈现好而不是堆砌**：后端每个框都返回匹配 soft-IoU、文字色、背景色、背景标准差、
   平面残差、笔画宽度、擦除策略建议。低 IoU（<0.7）是「原文与图上不一致」的可靠信号，要显眼。
4. 保留并做好现有能力：撤销/重做、操作计数、保存草稿、导出下载、覆盖区域、新增文字、
   拖动矢量文字、多页导航、以及两条必须始终可见的警示（编辑使数字签名失效；覆盖只是视觉覆盖不是安全脱敏）。

## 设计约束

- 中文界面。技术值（IoU、坐标、颜色值、字号）**按后端返回原样展示，不要自作主张格式化或换算**。
- 不引入 UI 框架和组件库（当前依赖只有 react / react-dom / pdfjs-dist，保持这样）。可以自己写 CSS。
- 深色/浅色不做要求，做好一种即可。
- 不要启动开发服务器或后端服务，也不要跑 docker。

## 验收（你自己要跑通）

```bash
cd apps/web
npm run typecheck   # tsc -b，必须无错误（注意：不是 tsc --noEmit）
npm run build
npm test
```

三条全绿才算完成。完成后在最后输出里简述你的设计思路、拆了哪些组件、以及你认为
还需要人工确认的地方。
