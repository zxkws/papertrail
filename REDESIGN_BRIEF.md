# 第二轮：重做入口页，把「图片直接编辑」这条路径呈现出来

上一轮你重做了工作区（左侧页面列表、检查器面板），但**入口页（`!doc` 时的空状态）一行没改**，
而那正是最需要改的地方。现在后端也扩了能力，入口的前提变了。

## 变了什么（后端已实现并有测试覆盖）

**以前只能传 PDF**，`accept="application/pdf"`，后端 `if not data.startswith(b"%PDF")` 直接 415。
这对本产品是荒谬的——它的招牌能力就是改扫描件和截图里的文字，却要求用户先自己把 PNG 转成 PDF。
（而且转 PDF 根本不会让图片变得可编辑，只是换了个容器。）

**现在可以直接上传图片**：PNG / JPEG / GIF / BMP / TIFF / WebP。服务端按魔数识别，
包成单页 PDF 走同一套页面模型，该页必然判定为**位图页**。页面尺寸按 200dpi 折算，
渲染回来与原图逐像素一致，改字不损失清晰度。用户的原图另存并单独记哈希，不被改动。

新增的前端契约（已经写好，直接用，不要改）：

- `DocumentInfo.origin`：上传的是图片时非空，含 `media_type` / `width_px` / `height_px` / `sha256` / `size_bytes`
- `api.downloadPng(versionId)`：把导出结果渲染成 PNG 的下载地址

## 你要做的

### 1. 入口页重做

现在是一个只写着「上传普通 PDF 开始」的落地页，主标题在讲「把改动留在操作轨迹里」。
它没有传达这个产品真正的分流逻辑，也完全没提图片。要让用户在上传前就明白：

- **矢量 PDF**（数字生成）→ 点选文字直接改，导出无损，文字仍可选中
- **扫描件 / 截图 / 照片**（PDF 里的位图页，或直接传图片）→ 先 OCR 识别，
  再擦掉原字、用匹配到的字体重绘像素

两条路径的能力和代价不同，用户应当在选择文件之前就有预期。文件选择器要接受
PDF 和图片两类（`accept` 要相应放开），并且让「可以直接拖图片进来」这件事显而易见。

### 2. 导出区补上图片下载

当 `doc.origin` 存在（说明用户上传的是图片）时，导出后除了「下载 PDF」还要给出
「下载 PNG」（用 `api.downloadPng(versionId)`）。传图进来的用户要的是图出去，不是 PDF。
非图片来源的文档不要显示这个入口。

### 3. 顺带

上传成功后，左上角文档信息区目前只显示页数和哈希前 16 位。图片来源的文档
可以把原图尺寸、类型一并呈现出来。

## 约束（与上一轮相同）

- 只改 `apps/web/`，不要动 `apps/api/`、`schemas`、Dockerfile、workflow
- `apps/web/src/lib/` 与 `types.ts` 是契约层，**保留现有导出签名与行为**，不要改
- 不引入 UI 框架或组件库，依赖保持 react / react-dom / pdfjs-dist
- **沿用项目既有的视觉语言**：`--ink` `#17201b`、`--acid` `#d7ff3f`、`--paper` `#f8f6ed`、
  `--line` `#bbbdb2`，方角、DM Mono 展示数值、Manrope 正文。上一轮你引入了 Material 风的
  蓝绿配色和圆角投影，与页面其余部分割裂，已被改回项目变量——这次请直接用变量
- 中文界面；技术值按后端返回原样展示，不要自作主张格式化
- 不要启动服务，不要跑 docker

## 验收

```bash
cd apps/web
npm run typecheck && npm run build && npm test
```

三条全绿。完成后说明你的信息架构思路，以及哪些地方需要人工在浏览器里确认。
